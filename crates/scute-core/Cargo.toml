[package]
name = "scute-core"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
semver = "1.0.27"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
scute-test-utils = { path = "../scute-test-utils" }
